/**
 * @foodo/tokens — platform-agnostic Kitchyn brand tokens.
 *
 * Shared between the web Tailwind config and the mobile NativeWind theme so the
 * brand stays identical across surfaces. Contains NO platform-specific code
 * (no react-dom, no react-native) — safe to import anywhere.
 */
export * from "./colors";
export * from "./radius";
export * from "./typography";
